# Machine-local Claude Code settings (session permission grants) — never commit
.claude/settings.local.json

# Per-task working notes (disposable task memory)
.claude/tasks/

# Mission-control run state (artifact URL + generated dashboard) — machine-local
.claude/mission-control.json
.claude/mission-control.html
